<!DOCTYPE html>
<html>
  <head>
	<title><%= @title %></title>
	<%= csrf_meta_tag %>
	<%= render 'layouts/stylesheets' %>
  <%= javascript_include_tag :defaults, "jquery.cycle.lite.min.js" %>
  </head>
  <body>
  <div id="line">
  </div>
  	<div class="container">
  		<div>
      <%= render 'layouts/header' %>
      </div>
      <section class="round">
          <%= yield %>
      </section>	
      <div>
        <%= render 'layouts/footer' %>
      </div>
  	</div>
  </body>
</html>